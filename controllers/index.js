const login = require('./loginControllers');
const user = require('./userControllers');
const category = require('./categoryControllers');
const post = require('./postControllers');

module.exports = {
  login,
  user,
  category,
  post,
};
